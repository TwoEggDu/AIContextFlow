# 给 Copilot Chat / ChatGPT 的协作准则（短版）

- 先读：`.project/ledger.json`（长期事实）
- 再读：`.project/decisions.json` 与引用的 accepted ADR（硬约束）
- 再读：`.project/current_intent.json`（本次任务的范围/禁止项）
- 只在 `in_scope` 的文件上改动；不要“顺手重构”
